# Expected Output

- `go run ./examples/go/memory` prints a `document_id` prefixed with `doc_`.
- Output includes `trace_id=trace_example_memory` and `cache_status=disabled`.
- The package test confirms ingestion, query response metadata, and citation counts remain stable.
