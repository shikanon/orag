# Expected Output

- `30_query.sh` returns a user-facing answer with citations and a trace ID for product review.
- `40_eval.sh` returns quality metrics such as `pairwise_accuracy`, `ndcg_at_k`, and `recall_at_k`.
- `45_optimize.sh` returns candidate retrieval settings and a selected best result.
- Product teams can use the outputs as launch-readiness evidence for the assistant scenario.
