# Expected Output

- `36_trace_lookup.sh` lists recent traces and prints detail for the saved trace ID.
- The self-check smoke returns `structuredContent.verdict`, evidence IDs, and a `trace_id`.
- Operational Skill prompts stay read-only unless an explicit apply approval is provided.
