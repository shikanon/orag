# Sample Input

Dataset item question: Which ORAG module returns trace metadata?

Expected evidence: Query responses include a `trace_id`, and trace lookup returns detail for that ID.

Optimization target: improve retrieval quality without increasing latency beyond the selected profile budget.
