# Expected Output

- `40_eval.sh` creates a dataset item and evaluation result with quality metrics such as `pairwise_accuracy`, `ndcg_at_k`, and `recall_at_k`.
- `45_optimize.sh` returns candidate profile/top-k settings and the selected best result.
- The output can be used as release evidence for retrieval configuration changes.
