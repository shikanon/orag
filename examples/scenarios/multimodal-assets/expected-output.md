# Expected Output

- `go run ./examples/scenarios/multimodal-assets` prints `scenario=multimodal-assets`.
- The output includes `asset_count=7`.
- Every asset prints `name`, `kind`, `filename`, `extension`, `url`, `upload_test`, `large_file_only`, and `usage`.
- The output includes `TestLongVideo.mp4` with `large_file_only=true`.
- `usage_dimensions` explains the users, input data, ORAG capabilities, and long-video upload-only success signal.
