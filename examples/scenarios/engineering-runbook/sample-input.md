# Sample Input

Engineer question: The query endpoint is slower than usual after a deploy. Which runbook steps should I check first, and what trace evidence should I attach?

Knowledge sources:

- API latency runbook.
- Query and retrieval architecture notes.
- Incident review that lists required evidence for escalation.

Engineering goal: get a grounded action list and inspect the trace that explains retrieval and generation behavior.
