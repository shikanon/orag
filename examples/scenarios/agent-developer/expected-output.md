# Expected Output

- `go run ./examples/scenarios/agent-developer` prints `scenario=agent-developer`.
- The output includes `document_id=doc_`, `answer=Found`, `citations=`, and `trace_id=trace_agent_developer`.
- `usage_dimensions` explains agent developers, MCP/Skill inputs, ORAG tool capabilities, and approval-boundary success signals.
- `recommended_next_steps` points agent developers to MCP client config, `make agent-sync-check`, and explicit approval boundaries.
