# Sample Input

Conversation turn: Explain how ORAG imports documents and why citations matter.

UI behavior: Render every `chunk` event immediately, attach citations when the `citations` event arrives, and close the turn on `done`.
